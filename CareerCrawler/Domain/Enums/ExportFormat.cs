namespace HHParser.Domain.Enums
{
    /// <summary>
    /// Specifies the data export format.
    /// This enum is used to prompt the user to select the desired format (CSV or JSON)
    /// during the data export process.
    /// </summary>
    public enum ExportFormat
    {
        Csv,
        Json
    }
}
